# {Tool name}

Last tested: {date and result, or never}.

## What it is for

The jobs this connection does for the owner.

## How the assistant reaches it

A connector built into Claude or Codex, a script in `Tools/`, or an export file. Say which, for each engine.

## Keys

The name of each key in `.env`. Never the value.

## Safe test

One read-only check that proves it works, and what a good result looks like.

## What the assistant may do

Read only, or the exact actions allowed. Anything that sends, spends, publishes or changes a live account needs a clear yes first.

## Gotchas

Anything that already went wrong once.

## How to disconnect

The steps to remove access.
